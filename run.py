"""
Файл запуска Flask-приложения в режиме разработки.

Используется как простая точка входа: создает приложение через фабрику
`create_app()` и поднимает встроенный dev-сервер Flask.
"""

from app import create_app

# Точка входа для локального запуска приложения.
app = create_app()

if __name__ == '__main__':
    # Встроенный сервер удобен для локальной разработки:
    # - автоматически перезапускается при изменениях кода (debug=True);
    # - показывает подробные ошибки в браузере.
    # Для production следует использовать WSGI-сервер (например, waitress/gunicorn).
    app.run(debug=True, host='0.0.0.0', port=5000)
